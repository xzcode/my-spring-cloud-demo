# Nacos Namespace: test
# Data ID: shared-redis.yml  Group: DEFAULT_GROUP

spring:
  data:
    redis:
      host: redis-test.internal
      port: 6379
      database: 0
      password: test_redis_password
      timeout: 3000ms
      lettuce:
        pool:
          max-active: 32
          max-idle: 16
          min-idle: 4
          max-wait: 3000ms
